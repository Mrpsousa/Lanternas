# Lanternas Projeto Social - Site

Site institucional do **Lanternas Projeto Social**.

## Subir localmente

```bash
cd noiz-site

# Desenvolvimento (hot-reload)
docker compose --profile dev up

# Produção (SSR)
docker compose --profile prod up
```